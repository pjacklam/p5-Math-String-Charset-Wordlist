This lists only the changes for the current release (v0.08) over the last
version, since CHANGES is now quite big:

  * modernize package (bundle Module::Install etc.)
  * add POD tests
  * we don't need to have Exporter in @ISA

